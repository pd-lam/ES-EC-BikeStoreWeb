@model IEnumerable<WebApp.Models.service>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/_Shared/_IndexLayout.cshtml";
}

@section title {Service Pack}

@section fieldName
{
    <th>ID</th>
    <th>Name</th>
    <th>Type</th>
}

<!-- RenderBody -->
@foreach (var item in Model)
{

    <tr>
        <td style="width: 50px">
            <span class="custom-checkbox">
                <input type="checkbox"
                       id="checkbox1"
                       name="options[]"
                       value="1">
                <label for="checkbox1"></label>
            </span>
        </td>
        <td style="width: 50px">
            @Html.DisplayFor(modelItem => item.service_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.service_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.service_type)
        </td>
        <td>
            <a href="@Url.Action("Edit", "Services",
                        new { id = item.service_id })">
                <i class='bx bxs-edit-alt float-end fs-4 me-1 ms-1'></i>
            </a>
            <a href="@Url.Action("Details", "ServicePacks",
                        new { id = item.service_id })">
                <i class='bx bx-detail float-end fs-4 me-1 ms-1'></i>
            </a>
        </td>
    </tr>
}
